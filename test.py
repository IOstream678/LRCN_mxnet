# from mxnet import nd
# X = nd.ones(shape=(3, 2, 2, 3, 2))
# Y = nd.ones(shape=(3, 2, 2, 3, 2))
# Z = nd.concat(X, Y)
# print(Z)
# print(X.split(axis=2, num_outputs=X.shape[2], squeeze_axis=1))
# from mxnet import gluon
# net = gluon.model_zoo.vision.get_model('vgg16', pretrained=True)
# print(net)
# from mxnet import gluon
# print(gluon.model_zoo.vision.get_model(name='alexnet', pretrained=True).features[:10
